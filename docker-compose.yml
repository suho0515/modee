version: '2.2'

networks:
  ros:
    driver: bridge

services:
  ros-master:
    image: modee:latest
    container_name: ros-master
    command: stdbuf -o L roscore
    networks:
      - ros
    restart: always

  cam_node:
    image: modee:latest
    container_name: cam_node
    depends_on:
      - ros-master
    networks:
      - ros
    environment:
      - "ROS_MASTER_URI=http://ros-master:11311"
      - "ROS_HOSTNAME=cam_node"
      - "DISPLAY=unix$DISPLAY"
    volumes: 
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
    command: >      
      bash -c "source /opt/ros/noetic/setup.bash &&
      rosrun usb_cam usb_cam_node _pixel_format:="yuyv" _autofocus:=false _focus:=500 _framerate:=30 _sharpness:=0 _contrast:=32"
    privileged: true
    restart: always

  modee-dev-container:
    image: modee:latest
    container_name: modee-dev-container
    networks:
      - ros
    environment:
      - "ROS_MASTER_URI=http://ros-master:11311"
      - "ROS_HOSTNAME=modee-dev-container"
      - "DISPLAY=unix$DISPLAY" 
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw 
      - ~/github/modee:/root/catkin_ws/src/modee
    command: tail -F anything
    privileged: true
    restart: always

  ocr-dev-container:
    image: ocr:latest
    container_name: ocr-dev-container
    networks:
      - ros
    environment:
      - "ROS_MASTER_URI=http://ros-master:11311"
      - "ROS_HOSTNAME=ocr-dev-container"
      - "DISPLAY=unix$DISPLAY" 
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw 
      - ~/github/ocr:/root/catkin_ws/src/ocr
    command: tail -F anything
    privileged: true
    restart: always

  inception-v3-dev-container:
    image: inception-v3:latest
    container_name: inception-v3-dev-container
    networks:
      - ros
    environment:
      - "ROS_MASTER_URI=http://ros-master:11311"
      - "ROS_HOSTNAME=inception-v3-dev-container"
      - "DISPLAY=unix$DISPLAY" 
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw 
      - ~/github/inception_v3:/root/catkin_ws/src/inception_v3
    command: tail -F anything
    privileged: true
    restart: always